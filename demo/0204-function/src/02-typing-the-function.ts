// # 为函数定义类型
namespace typeTheFunctions {
  // ## demo1: 为函数添加类型
  function add(x: number, y: number): number {
    return x + y;
  }

  const myAdd = (x: number, y: number): number => {
    return x + y;
  }

  
}